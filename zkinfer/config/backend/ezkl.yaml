# config/backend/ezkl.yaml

name: ezkl

proving_cache:
  # Reuse proving artifacts across runs.
  enabled: true

  # Force regeneration even if cache exists.
  overwrite: false

  # Cache namespace under storage/cache/
  namespace: ezkl