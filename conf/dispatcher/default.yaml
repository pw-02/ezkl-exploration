# Dispatcher service config
host: "54.188.84.18" #"localhost"  # IP address of the dispatcher host
port: 50051
max_workers: 1
num_prover_workers: 1 # Number of workers for the prover - used for logging only