defaults:
  - workload: mnist_classifier
  - _self_

execution:
  split_mode: none
  ops_per_chunk: 1

jobs:
  scheduler: lpt

launch:
  num_workers: 1
  coordinator_host: localhost
  coordinator_port: 50051
  coordinator_ready_timeout_sec: 30
  worker_startup_delay_sec: 1

paths:
  runs_dir: experiments/runs